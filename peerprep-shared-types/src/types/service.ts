// Enum for service names to ensure consistency
export enum ServiceNames {
  MATCHING_SERVICE = "matching-service",
  COLLABORATION_SERVICE = "collaboration-service",
  USER_SERVICE = "user-service",
  API_GATEWAY = "api-gateway",
}
